#ifndef MERGE_SORT
#define MERGE_SORT
#include <vector>
using namespace std;

vector<int> merge(vector<int> vect, int p, int q, int r);

vector<int> mergeSort(vector<int> vect, int p, int q);

#endif
